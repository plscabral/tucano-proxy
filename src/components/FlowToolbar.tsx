import type { Flow } from "@/lib/types";
import { Stub } from "./_stub";
export default function FlowToolbar(_props: { count: number; flows: Flow[]; onCompare?: () => void }) {
  return <Stub name="FlowToolbar" className="h-9 border-b border-border/60" />;
}
