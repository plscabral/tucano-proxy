import type { Flow } from "@/lib/types";
import { Stub } from "./_stub";
export default function Composer(_props: { onClose: () => void; initialFlow: Flow | null }) {
  return <Stub name="Composer" className="h-full" />;
}
