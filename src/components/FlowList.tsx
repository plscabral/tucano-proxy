import type { Flow } from "@/lib/types";
import { Stub } from "./_stub";
export default function FlowList(_props: { flows: Flow[]; onCompare?: () => void; onOpen?: (id: string) => void }) {
  return <Stub name="FlowList" className="h-full" />;
}
